server:
  port: 9093

eureka:
  client:
    service-url:
      defaultzone: http://localhost:8761/eureka/

spring:
  application:
    name: USER-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/user_db?useSSL=false&createDatabaseIfNotExist=true
    username: root
    password: 406290@LAkshmi
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect